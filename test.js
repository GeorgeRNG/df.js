const { Player } = require('./df')

Player.on('Join',player => {
    // player.SendMessage(60)
    player.SendMessage('does it work lol?')
    // player.SendMessage('80')
    // player.SendMessage(80)
}).json('GeorgeRNG')

setTimeout(() => {},200000)